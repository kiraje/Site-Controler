{
  "name": "multi-site-control-panel",
  "version": "1.0.0",
  "description": "Firebase-based control panel for managing multiple sites",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npx serve dist",
    "server": "node server.js",
    "server:dev": "nodemon server.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "firebase": "^10.7.0"
  }
}